export const getDate_format = function() {
    
}

export const set_add = function(type: string, tit: string): boolean {
    return type === tit
    // let secondNum = 0
    // if(dis) {
    //     let secondNum = now_day.getSeconds() + distance
    //     dis = 0
    // }
    // return 0
}